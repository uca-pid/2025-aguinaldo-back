meta {
  name: Approve Doctor - Invalid ID
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/admin/approve-doctor/invalid-uuid
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 400
}

tests {
  test("Should return 400 for invalid doctor ID", function() {
    expect(res.getStatus()).to.equal(400);
  });
}