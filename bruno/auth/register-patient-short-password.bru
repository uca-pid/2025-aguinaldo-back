meta {
  name: Register Patient - Short Password
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/auth/register/patient
  body: json
  auth: none
}

body:json {
  {
    "email": "test@example.com",
    "dni": 12345678,
    "password": "123",
    "name": "John",
    "surname": "Doe"
  }
}

assert {
  res.status: eq 400
}

tests {
  test("Should reject short password", function() {
    expect(res.getStatus()).to.equal(400);
  });
}