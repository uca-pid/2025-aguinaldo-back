meta {
  name: Sign Out
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/auth/signout
  body: none
  auth: none
}

headers {
  Refresh-Token: {{refreshToken}}
}

assert {
  res.status: eq 200
}

tests {
  test("Should sign out successfully", function() {
    expect(res.getStatus()).to.equal(200);
  });
}