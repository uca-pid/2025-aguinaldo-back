meta {
  name: Register Patient - Invalid Email
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/auth/register/patient
  body: json
  auth: none
}

body:json {
  {
    "email": "invalid-email",
    "dni": 12345678,
    "password": "password123",
    "name": "John",
    "surname": "Doe"
  }
}

assert {
  res.status: eq 400
}

tests {
  test("Should reject invalid email format", function() {
    expect(res.getStatus()).to.equal(400);
  });
}