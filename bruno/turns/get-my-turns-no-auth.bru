meta {
  name: Get My Turns - No Auth
  type: http
  seq: 29
}

get {
  url: {{baseUrl}}/api/turns/my-turns
  body: none
  auth: none
}

assert {
  res.status: eq 401
}

tests {
  test("Should return 401 without authentication", function() {
    expect(res.getStatus()).to.equal(401);
  });
}