<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <changeSet id="0004-01-seed-roles" author="MediBook_Admin" runOnChange="false" runAlways="false">
        <sql>
            INSERT INTO public.roles (code, name) 
            VALUES ('PATIENT', 'Paciente') 
            ON CONFLICT (code) DO NOTHING;
            
            INSERT INTO public.roles (code, name) 
            VALUES ('DOCTOR', 'Médico') 
            ON CONFLICT (code) DO NOTHING;
            
            INSERT INTO public.roles (code, name) 
            VALUES ('ADMIN', 'Administrador') 
            ON CONFLICT (code) DO NOTHING;
        </sql>
    </changeSet>

    <!-- Evitar duplicados en re-ejecución (si ya existen) -->
    <changeSet id="0004-02-roles-unique-guard" author="MediBook_Admin" context="guard">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM roles WHERE code IN ('PATIENT','DOCTOR','ADMIN') HAVING COUNT(*)=3
            </sqlCheck>
        </preConditions>
        <!-- Si ya están, se marca como ejecutado sin reinsertar -->
        <comment>Guarda semilla roles si ya existen</comment>
    </changeSet>

</databaseChangeLog>
